# ============================================================
# Test selection
# ------------------------------------------------------------
# The BEGIN/END SELECTION block is auto-synced from test_items/test_*.py
# whenever nvme_raid_test.py runs (or: python nvme_raid_test.py --sync-selection).
#
# Uncomment a line to run that case. Writing order among uncommented lines
# is the run order. All discovered cases stay listed for easy toggle.
#
# Example — run only lawdisk:
#   uncomment "lawdisk", keep the others commented.
#
# Naming:
#   test_smoke_03_lawdisk.py  ->  lawdisk
#   test_foo.py               ->  foo
#
# To add a new case:
#   1. Add test_items/test_<name>.py (or test_smoke_NN_<name>.py)
#   2. Run sync (or any test run) — the new name appears below as "# <name>"
#   3. Uncomment it to enable; add a [<name>] param block if needed
# ============================================================

# === BEGIN SELECTION (auto-synced; uncomment a line to run) ===
# reboot
# dc
 lawdisk
 filesystem
 mix
# === END SELECTION ===


# ============================================================
# Per-item parameters (only keys used by that case)
# ============================================================

[reboot]
FIO_CYCLES      = 10
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME =

[dc]
FIO_CYCLES      = 5
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME =

[lawdisk]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = yes
MONITOR_RUNTIME = 1000

[filesystem]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = yes
MONITOR_RUNTIME = 1000

[mix]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME =
