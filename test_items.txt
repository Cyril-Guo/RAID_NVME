# ============================================================
# CLI 测试项选择
# ------------------------------------------------------------
# 命名：test_cli_<NN>_<name>
# 行格式：<完整用例名> <执行序号>
# 行首 # 表示不跑
#
# 用例约定：每一步用 cli_step.run_step / run_steps
#   - 完整打印将要执行的命令
#   - 完整打印命令输出（stdout/stderr）与返回码
#   - 严格按列表顺序逐步执行，不做并行/旁路监控
# ============================================================

# === BEGIN SELECTION（自动同步；完整用例名 + 执行序号，# 表示不跑）===
# === END SELECTION ===
