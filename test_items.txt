# ============================================================
# Test selection (whitelist)
# ------------------------------------------------------------
# Put one item name per line ABOVE the first [section].
# Run order = writing order. Lines starting with # are comments.
# Empty list = run nothing (safe default for destructive tests).
#
# Available names are auto-discovered from test_items/test_*.py
#   test_smoke_03_lawdisk.py  ->  lawdisk
#   test_foo.py               ->  foo
#
# To add a new case later:
#   1. Add test_items/test_<name>.py (or test_smoke_NN_<name>.py)
#   2. Add "<name>" to this whitelist when you want to run it
#   3. Optional: add a [<name>] override block only if params differ
#      from [defaults]
# ============================================================

# lawdisk
# mix


[defaults]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME =
FIO_CYCLES      = 10


# ============================================================
# Per-item overrides (only keys that differ from [defaults])
# ============================================================

[reboot]
FIO_CYCLES      = 10

[dc]
FIO_CYCLES      = 5

[lawdisk]
STRESS_MONITOR  = yes
MONITOR_RUNTIME = 1000

[filesystem]
STRESS_MONITOR  = yes
MONITOR_RUNTIME = 1000

[mix]
STRESS_MONITOR  = no
