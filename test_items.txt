# ============================================================
# PowerCycle 测试项选择
# ------------------------------------------------------------
# BEGIN/END SELECTION 块会在运行 nvme_raid_test.py 时自动同步
# （也可手动：python nvme_raid_test.py --sync-selection）。
#
# 本分支仅保留电源循环相关用例：
#   test_powercycle_00_env_prepare / test_powercycle_01_reboot / test_powercycle_02_dc
# reboot / dc 必须单独跑（不可与其它用例同一次 selection）。
# 行首 # 表示不跑；去掉 # 即启用。
# ============================================================

# === BEGIN SELECTION（自动同步；完整用例名 + 执行序号，# 表示不跑）===
# test_powercycle_00_env_prepare 0
test_powercycle_01_reboot 1
# test_powercycle_02_dc 2
# === END SELECTION ===


# ============================================================
# 各用例参数
# FIO_CONFIG 指向 IO_Stress/ 下 CSV
# FIO_CYCLES：电源循环次数（仅 reboot / dc）
# IGNORE_ERROR：MachineCheck 不一致时是否继续（yes/no）
# ============================================================

[test_powercycle_00_env_prepare]
# Stop QEMU if running, unload draid, return vfio devices to host,
# then CSD flash clear + dpraid/draid reload + VD/PD clear.

[test_powercycle_01_reboot]
FIO_CYCLES      = 100
FIO_CONFIG      = Input_Config_reboot_4k.csv
IGNORE_ERROR    = yes
FIO_DISKS       =

[test_powercycle_02_dc]
FIO_CYCLES      = 5
FIO_CONFIG      = Input_Config_dc_4k.csv
IGNORE_ERROR    = yes
FIO_DISKS       =
