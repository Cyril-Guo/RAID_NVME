# ============================================================
# Test selection
# ------------------------------------------------------------
# The BEGIN/END SELECTION block is auto-synced from test_items/test_*.py
# whenever nvme_raid_test.py runs (or: python nvme_raid_test.py --sync-selection).
#
# Uncomment a line to run that case. Line order among uncommented names
# is the run order — reorder freely; sync keeps your order and only adds
# newly discovered names (as "# name") or drops removed ones.
#
# Example — run only lawdisk:
#   uncomment "lawdisk", keep the others commented.
#
# Naming:
#   test_smoke_03_lawdisk.py  ->  lawdisk
#   test_foo.py               ->  foo
#
# To add a new case:
#   1. Add test_items/test_<name>.py (or test_smoke_NN_<name>.py)
#   2. Run sync (or any test run) — the new name appears below as "# <name>"
#   3. Uncomment it to enable; add a [<name>] param block if needed
# ============================================================

# === BEGIN SELECTION (auto-synced; reorder / uncomment to choose run order) ===
# reboot
# dc
lawdisk
mix
filesystem
# === END SELECTION ===


# ============================================================
# Per-item parameters (only keys used by that case)
# ============================================================

[reboot]
FIO_CYCLES      = 10
IGNORE_ERROR    = no
FIO_DISKS       =

[dc]
FIO_CYCLES      = 5
IGNORE_ERROR    = no
FIO_DISKS       =

[lawdisk]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME = 21600

[filesystem]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME = 21600

[mix]
IGNORE_ERROR    = no
FIO_DISKS       =
STRESS_MONITOR  = no
MONITOR_RUNTIME =
