---
description: 所有代码改动只能在 192.168.22.226 完成；Windows 仅同步
alwaysApply: true
---

# Linux 开发机工作流（强制）

目的：避免 Windows 上的 CRLF 与 TSD 加密污染 git 对象；禁止本机误改。

## 主机

- Host: `192.168.22.226`
- User: `root`
- 工作目录: `/root/Cyril/Cursor/RAID_NVME`
- 远程仓库: `git@github.com:Cyril-Guo/RAID_NVME.git`
- 密码：**不要写入仓库**；本机用环境变量 `DEV_HOST_PASS`，并尽快改用 SSH key

## 硬性规则

1. **所有业务代码的编辑、测试、`git commit`、`git push` 只能在 `192.168.22.226` 进行**
2. **Windows 本机只允许**：`git fetch` / `git pull --ff-only` / `python ci/dev_host.py sync-local`，以及只读查看
3. **禁止**在 Windows worktree 使用 Write / StrReplace / EditNotebook / 手工保存去改 `RAID_NVME` 下任何已跟踪文件
4. Agent 若需改代码：必须 SSH/SFTP 到 226 的 `/root/Cyril/Cursor/RAID_NVME` 再改，然后在 226 上 commit/push；最后 Windows 执行 `sync-local`
5. 若 Windows 出现 `M` / 脏工作区：立刻 `python ci/dev_host.py sync-local SMOKE` 丢弃本机改动，不要在本机 commit

## 标准流程

1. 在 226 上拉取并编辑、commit、push
2. Windows：`python ci/dev_host.py sync-local SMOKE`（或 `git pull --ff-only`）只对齐远端

## 辅助命令

```bash
# Windows（只同步，不编辑）
python ci/dev_host.py sync-local SMOKE
python ci/dev_host.py status          # 查看 226 状态
python ci/dev_host.py pull SMOKE      # 让 226 拉最新

# 226 上日常
cd /root/Cyril/Cursor/RAID_NVME
git fetch origin && git checkout SMOKE && git pull --ff-only
# ... edit on 226 only ...
git add -A && git commit && git push
# hooksPath=.githooks 必须已启用（bootstrap/pull 会自动配置）
```

## GitHub 访问

226 使用 `/root/.ssh/id_ed25519_raid_nvme` 访问 GitHub；公钥需加到该仓库 **Deploy keys（允许写）**。
