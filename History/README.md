# History

归档目录（**不是**活跃用例）：

- 旧无后缀 `Input_Config_*.csv`
- CI 已移除的 `reboot` / `dc` / `basic_io` / `basic_rebuild_io` 脚本与 CSV
- `powercycle_launch.py` 及对应单测

活跃 CI 用例统一为连续编号：

`test_ci_00_env_prepare` … `test_ci_08_random_io_512`

电源循环（reboot/dc）完整能力在 Git 分支 **`PowerCycle`**。
